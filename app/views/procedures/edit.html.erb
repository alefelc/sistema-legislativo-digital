<div class="initiators-modal"></div>
<div id="procedure-new" class="<%= 'procedure-dispatch-edit' if @procedure.is_dispatch? %>">
  <% if @procedure.is_dispatch? %>
    <h2> Editando despacho nro <%= @procedure.id %> </h2>
  <% else %>
    <h2> Editando trámite nro <%= @procedure.id %> </h2>
  <% end %>

  <%= form_for @procedure, multipart: true do |form| %>
    <div class="box box-primary">
      <div class="box-body">
        <%= render 'form', f: form %>
      </div>
    </div>
  <% end %>
</div>

<!--         <div class="box-footer">
          <div class="pull-right">
            <%= link_to procedures_path, class: 'btn btn-default' do %>
              <div class="fa fa-times-circle"></div>
              Cancelar
            <% end %>
            <button class="btn btn-success btn-lg" type="<%= :submit %>">
              <div class="fa fa-save fa-lg"></div>
              <%= t('.submit') %>
            </button>
          </div>
        </div>
 -->
