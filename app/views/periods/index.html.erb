<h2>
  Periodos Legislativos
</h2>
<div class="box box-primary">
  <div class="box-header"> <%= render 'header_buttons' %> </div>
  <div class="box-body">
      <% @periods.each do |p| %>
        <div class="row col-md-12">
          <div class="col-md-6">
            <h3> Período </h3>
            <label> <%= "#{p.desde} - #{p.hasta}" %> </label>
          </div>
          <div class="col-md-6" style="margin-top: 30px">
            <%= link_to '', edit_period_path(p), class: 'btn btn-warning btn-lg fa fa-lg fa-edit pull-right' %>
          </div>
          <hr class="col-md-12">
        </div>
      <% end %>
  </div>
</div>
