<div class="row">
  <div class="col-md-6">
    <h2>
      Sesión nro: <%= @session.number %>
    </h2>
  </div>
</div>

<div class="box box-primary">
  <div class="box-header with-border">
    <h3 class="col-md-6 box-title">Información de la sesión</h3>
    <div class="col-md-6">
      <div class="pull-right">
        <%= link_to edit_session_path(@session), class: 'btn btn-lg btn-warning' do %>
          <i class="fa fa-pencil"></i>
          Editar sesión
        <% end -%>
      </div>
    </div>
  </div>

  <%= form_for @session do |f| %>

    <div class="box-body row">
      <div class="col-md-6">
        <div class="form-group">
          <%= f.label :number %>
          <%= f.text_field :number, type: :number, class: 'form-control', disabled: true %>
        </div>
        <div class="form-group">
          <%= f.label :place %>
          <%= f.text_field :place, class: 'form-control', disabled: true %>
        </div>
        <div class="form-group">
          <%= f.label :session_type %>
          <%= f.select :session_type, session_types, {}, class: 'form-control', disabled: true %>
        </div>
        <div class="form-group">
          <%= f.label :secret %>
          <%= f.check_box :secret, disabled: true %>
        </div>
      </div>

      <div class="col-md-6">
        <div class="form-group">
          <%= f.label :started_at %>
          <%= f.text_field :started_at, type: 'datetime-local', class: 'form-control', disabled: true %>
        </div>
        <div class="form-group">
          <%= f.label :finished_at %>
          <%= f.text_field :finished_at, type: 'datetime-local', class: 'form-control', disabled: true %>
        </div>
        <div class="form-group">
          <%= f.label :observation %>
          <%= f.text_field :observation, class: 'form-control', disabled: true %>
        </div>
      </div>
    </div>

  <% end -%>
</div>
