#particular
  #particular-edit

  - if current_user.present?
    %button#btn-new.btn.btn-primary
      %span.glyphicon.glyphicon-plus-sign{ "aria-hidden": true }
      Nueva Petición

  %table.datatable-keyevents.table.table-striped.table-bordered.table-hover#particular-table{style: "table-layout: fixed; word-wrap: break-word;", "data-source" => particulars_path(format: :json)}
    %thead
      %tr
        %th= t('.table.particular')
        %th= t('.table.fojas')
        %th= t('.table.iniciadores')
        %th= t('.table.asunto')
        %th= t('.table.observaciones')
        %th= t('.table.actualizado')
        %th
    %tbody
